# Proto compatibility QA fixture

The `ModelRoute` contract is introduced by the PR fixture. Compatibility checks should treat fields added within the same PR as new API surface, not as fields that existed on the base branch.
