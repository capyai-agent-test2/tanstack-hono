openapi: 3.1.0
info:
  title: Range context fixture
  version: 1.0.0
paths:
  /same-pr/v4/items:
    get:
      operationId: listSamePrItemsV4
      responses:
        "200":
          description: Items introduced by this PR
