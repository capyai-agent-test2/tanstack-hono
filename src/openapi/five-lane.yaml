openapi: 3.1.0
info:
  title: Range context fixture
  version: 1.0.0
paths:
  /same-pr/v2/items:
    get:
      operationId: listSamePrItemsV2
      responses:
        "200":
          description: Items introduced by this PR
