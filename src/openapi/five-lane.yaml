openapi: 3.1.0
info:
  title: Range context fixture
  version: 1.0.0
paths:
  /same-pr/v5/items:
    get:
      operationId: listSamePrItemsV5
      responses:
        "200":
          description: Items introduced by this PR
